server:
  port: 8001
      
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/qa
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true